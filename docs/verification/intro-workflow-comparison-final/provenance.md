# Final corrected-run provenance

This is a new final evidence directory. The previously recorded corrected
attempt under `../intro-workflow-comparison-corrected/` is immutable evidence
and is not rerun or represented as architectural evidence. The original pilot
under `../intro-workflow-comparison/` is also unchanged.

The final adapter explicitly embeds the exact production operation schema and
important-agent checklist, keeps real production instrument-writer identities
for score writes, and retains one Orchestrator identity for style/concept/plan/
verification. It also provides an experiment-private advisory MIDI loader and
an offline end-to-end fixture using a synthetic model. No live provider call
is permitted until this source, its production-module hashes, and offline
fixture results are reviewed.
